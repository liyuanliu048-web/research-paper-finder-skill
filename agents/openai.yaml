interface:
  display_name: "Research Paper Finder"
  short_description: "Find and deduplicate research papers"
  default_prompt: "Use $research-paper-finder to build a topic profile, run a broad baseline search across OpenAlex, Semantic Scholar, and arXiv, and rerun with a focused profile when the first pass is noisy."
